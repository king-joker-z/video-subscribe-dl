---
plan: 02.2-02
phase: "02.2"
status: complete
date: "2026-04-07"
self_check: PASSED
key-files:
  created:
    - web/api/metrics_test.go
  modified:
    - internal/scheduler/bscheduler/scheduler.go
    - internal/scheduler/dscheduler/scheduler.go
    - internal/scheduler/phscheduler/scheduler.go
    - internal/scheduler/scheduler.go
    - web/api/metrics.go
    - web/api/router.go
    - web/server.go
    - cmd/server/main.go
---

# Plan 02.2-02 Execution Summary

**Phase:** 02.2-observability
**Plan:** 02.2-02
**Status:** Complete
**Date:** 2026-04-07

## Tasks Completed

### T1–T3 — Sub-schedulers: lastCheckAt tracking (bscheduler, dscheduler, phscheduler)
- Added `lastCheckAt time.Time` + `lastCheckMu sync.RWMutex` fields to BiliScheduler, DouyinScheduler, PHScheduler
- Added `LastCheckAt()` (RLock) / `setLastCheckAt()` (Lock) methods to all 3 schedulers
- Added `defer s.setLastCheckAt(time.Now())` as first statement in each `CheckSource()` — covers all code paths including early-return via defer semantics

### T4 — Top-level Scheduler: LastCheckAt proxy methods
- Added `GetBiliLastCheckAt()`, `GetDouyinLastCheckAt()`, `GetPHLastCheckAt()` proxy methods on `*Scheduler`
- All three methods have nil-guard returning `time.Time{}` if sub-scheduler is nil

### T5 — MetricsHandler: 3 new Prometheus metric groups
- Added `getSchedulerLastCheck map[string]func() time.Time` field + `SetSchedulerLastCheckFunc(platform, fn)` setter
- `HandlePrometheus` now emits (alphabetical: bilibili→douyin→pornhub):
  - `vsd_downloads_completed_total{platform="..."}` — per-platform completed counter
  - `vsd_downloads_failed_total{platform="..."}` — per-platform failed counter
  - `vsd_scheduler_last_check_timestamp{platform="..."}` — Unix timestamp (0 if never checked)
- Zero-time guard: `if t := fn(); !t.IsZero() { ts = t.Unix() }` — prevents -62135596800 output

### T6 — Router: SetSchedulerLastCheckFunc proxy
- Added `SetSchedulerLastCheckFunc(platform, fn)` method on `*Router` delegating to `rt.metrics`

### T7 — Server: field + setter + setupRoutes wiring
- Added `getSchedulerLastCheck map[string]func() time.Time` to `Server` struct
- Added `SetSchedulerLastCheckFunc` setter with lazy-init map
- `setupRoutes()` iterates the map and calls `s.apiRouter.SetSchedulerLastCheckFunc` for each platform

### T8 — cmd/server/main.go wiring
- Three `server.SetSchedulerLastCheckFunc(platform, fn)` calls added after `sched.Start()`

### T9 — web/api/metrics_test.go (4 tests)
- `TestHandlePrometheus_PlatformCounters` — verifies bilibili=1, douyin/pornhub=0 present, HELP/TYPE lines, flat counter preservation
- `TestHandlePrometheus_SchedulerLastCheck_Zero` — verifies zero time renders as `0` not -62135596800
- `TestHandlePrometheus_SchedulerLastCheck_NonZero` — verifies non-zero timestamp passes through correctly
- `TestHandlePrometheus_MethodNotAllowed` — verifies POST returns 405

## Files Changed

- `internal/scheduler/bscheduler/scheduler.go` — lastCheckAt fields + methods + defer
- `internal/scheduler/dscheduler/scheduler.go` — lastCheckAt fields + methods + defer
- `internal/scheduler/phscheduler/scheduler.go` — lastCheckAt fields + methods + defer
- `internal/scheduler/scheduler.go` — 3 GetXxxLastCheckAt() proxy methods
- `web/api/metrics.go` — new field, setter, 3 Prometheus metric groups
- `web/api/router.go` — SetSchedulerLastCheckFunc proxy
- `web/server.go` — field, setter, setupRoutes wiring
- `cmd/server/main.go` — 3 SetSchedulerLastCheckFunc calls
- `web/api/metrics_test.go` — NEW: 4 HTTP tests for Prometheus endpoint
