# TODO List — video-subscribe-dl

> 按优先级排列，一个一个来。当前进行中的任务会标注 🔄。

---

## 工作规范

- ⚠️ **构建/测试全部走 GitHub CI，不在本地执行 `go build` / `go test`**
- 代码改完后 push 到 GitHub，等 CI 结果
- git push 前需经宇融确认

---

## 🔄 进行中

### [P0] scheduler 重构：顶层抖音逻辑委托给 dscheduler
- 子 Agent 正在执行，完成后自动通知
- 目标：删除旧抖音文件 ~1100 行，统一委托架构
- ⚠️ 注意：子 Agent 完成后不要本地 build，直接 commit + 等 CI

---

## 待处理队列（按执行顺序）

### [1] Bug：手动迁移本地文件后仍触发重复下载（抖音）
- 现象：本地已下载的文件手动移走后，系统判断未下载，再次触发下载
- 排查方向：下载状态如何判断（DB 记录 vs 文件存在）、迁移后 DB 状态是否同步
- 预期行为：DB 记录已 completed 则不重新下载，与文件是否存在解耦

### [2] 下载速度慢
- 现象：整体下载速度低，瓶颈不明
- 排查方向：
  - 分块并行参数配置（chunk size / 并发数）
  - 限速配置是否过于保守
  - B站 DASH 合并（ffmpeg）耗时
  - 抖音下载限流器参数
  - 网络层 vs 处理层瓶颈区分

### [3] 双平台环境彻底隔离（B站 vs 抖音）
- Cookie 各自独立，互不干扰
- 限流器（RateLimiter）各自独立
- 订阅下载任务互不影响（队列/并发/冷却均隔离）
- 验证 dscheduler 重构完成后是否已满足，不足则补充

### [4] 前后端 API 交互问题 + 前端 UI 优化
- API 交互问题（待列举具体接口）
- 前端 UI 展示问题（待截图或描述具体页面）
- 可分批处理，每次聚焦 1-2 个页面

### [5] Bug 专项修复（全量代码审查）
- 由 JARVIS 自主全量审查代码，整理 bug 清单后逐一修复
- 重点模块：下载流程、状态机、并发安全、错误处理、边界条件
- 穿插在各任务间进行

---

## 完成记录

- [x] B站调度逻辑抽离到 bscheduler 子包（v2.1.1）
- [x] dscheduler 子包实现完成（2026-03-19）
